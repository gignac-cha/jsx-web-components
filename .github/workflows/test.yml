name: Test

on:
  push:
    branches:
      - prototype

jobs:
  test:
    runs-on: ubuntu:latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: pnpm
      - run: pnpm install
